{
	"name": "nuxt-app",
	"private": true,
	"scripts": {
		"build": "nuxt build",
		"dev": "nuxt dev",
		"generate": "nuxt generate",
		"preview": "nuxt preview",
		"postinstall": "nuxt prepare"
	},
	"dependencies": {
		"nuxt": "^3.4.2",
		"incan-gold-core": "workspace:*"
	},
	"devDependencies": {
		"@types/node": "^18",
		"jest": "^29.5.0"
	}
}
