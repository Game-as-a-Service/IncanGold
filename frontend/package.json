{
	"name": "frontend",
	"private": true,
	"scripts": {
		"build": "nuxt build",
		"dev": "nuxt dev",
		"generate": "nuxt generate",
		"preview": "nuxt preview",
		"postinstall": "nuxt prepare"
	},
	"dependencies": {
		"normalize.css": "^8.0.1",
		"nuxt": "^3.4.2",
		"ramda": "^0.29.0"
	},
	"devDependencies": {
		"@types/node": "^18",
		"@types/ramda": "^0.29.2",
		"jest": "^29.5.0"
	}
}
